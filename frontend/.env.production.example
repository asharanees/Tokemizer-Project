# Set in Amplify environment variables (do not commit secrets)
# API Gateway custom domain
VITE_API_BASE_URL=https://api.example.com

# Optional direct control endpoint used when backend EC2 is down
VITE_EC2_CONTROL_URL=https://example.lambda-url.us-east-1.on.aws
